from django.conf.urls import patterns, include, url
from . import views

urlpatterns = patterns('',
	url(r'^post/new/$', views.post_new, name='post_new'),
	url(r'^post/new/webscrape.views.post_detail$', views.post_detail, name="post_detail")
)
